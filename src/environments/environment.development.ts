// Environnement de DÉVELOPPEMENT (substitué au build via fileReplacements).
// apiUrl relatif ('') : le proxy (proxy.conf.json) route /api -> http://localhost:8080.
export const environment = {
  production: false,
  apiUrl: ''
};
